{
  "name": "starpay-erp-docs",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx mintlify dev",
    "build": "npx mintlify export --output _site.zip && rm -rf _site && unzip -o _site.zip -d _site",
    "broken-links": "npx mintlify broken-links"
  },
  "devDependencies": {
    "mintlify": "latest"
  }
}
